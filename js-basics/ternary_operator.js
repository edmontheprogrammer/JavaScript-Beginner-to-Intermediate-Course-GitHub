// Ternary Operator
// If a customer has more than 100 points, they are a 'gold'
// customer, otherwise, they are a 'silver' customer. 

// 'point' number of points customer has 
let points = 90;
// 'type' is the type of customers 
let type = points > 100 ? 'gold' : 'silver';
console.log(type);
