// Removing Elements: 

const numbers = [1, 2, 3, 4];


// You can remove elements from the End
// const last = numbers.pop();
// console.log(numbers);
// console.log(last);

// You can remove elements from the Beginning of the array using the
// shift() method
// const first = numbers.shift();
// console.log(first);

// You can remove an element from the middle of the array
numbers.splice(2, 2);
console.log(numbers);

