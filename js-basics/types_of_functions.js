// Example of a function that performs a task
// this function is peforming a task, the task is to display something on 
// the console. 
function greet(name, lastName) {
    console.log('Hello ' + name + ' ' + lastName);
}

// example of a function that calcualtes a value. 
function square(number) {
    return number * number;
}

// this function call 'square(2)' returns a value, 4
// let number = square(2);

// Note 1: we have two function calls in this line 'log()' and 'square()'
console.log(square(2));

// Note 2: a function is a set of statments that either perform a task or 
// calculates and returns a value. 

// Note 3; A real world application is bascially a collections of hundreds
//   or thousands of functions working together to provide the
// functionality of that application. 