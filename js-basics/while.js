for (let i = 0; i < 5; i++) {
    if (i % 2 !== 0) console.log(i);
}
// Note 1; One key difference between a while loop and a for
// loop is that in for loops the loop variable is part of the 
// loop variable itself. But in while loops we have to declare
// the loop veriable externally. 

let i = 0; 